version: 2

models:
  - name: dim_clientes
    description: Essa é a dimensão com informações do clientes
    columns:
      - name: cod_cliente
        description: Essa é a chave primaria da dimensão clientes.
        tests:
          - unique
          - not_null