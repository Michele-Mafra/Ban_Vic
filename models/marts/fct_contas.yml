version: 2
models:
  - name: fct_contas_dados
    description: Tabela com fatos de contas cada, linha descreve informações da conta.
    columns:
      - name: num_conta 
        description: Essa é a chave primaria da fct conta.
        tests:
          - not_null
          - unique