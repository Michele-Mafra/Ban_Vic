version: 2

models:
  - name: dim_agencia
    description: Essa é a dimensão com informações da agencia
    columns:
      - name: cod_agencia
        description: Essa é a chave primaria da dimensão agencia.
        tests:
          - unique
          - not_null

